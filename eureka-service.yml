logging:
  level:
    com:
      netflix:
        discovery: 'OFF'
        eureka: 'OFF'

debug: false
trace: false

spring:
  application:
    name: eureka-service

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

---
spring:
  profiles: development

server:
  port: 8761